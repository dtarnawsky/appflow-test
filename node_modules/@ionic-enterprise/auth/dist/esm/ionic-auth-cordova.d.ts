import { AuthResult, IIonicAuth } from './interfaces';
import { IonicBaseAuth } from './base-auth';
export declare class IonicNativeAuth<IdToken extends {} = any> extends IonicBaseAuth<IdToken> implements IIonicAuth<IdToken> {
    protected logTag: string;
    protected internalGetToken(codeName: string, code: string, grantType: string, verifier: any, scope: string | undefined): Promise<AuthResult>;
    internalHandleCallback(url: string, _externalCallback: boolean): Promise<AuthResult>;
    refreshSession(tokenName?: string): Promise<void>;
    protected showUrl(url: string, options?: {
        hide?: boolean;
    }): Promise<any>;
}
