/**
 * @hidden
 */
export interface StorageProvider {
    get(key: string): Promise<string | null>;
    set(key: string, value: string): Promise<void>;
    remove(key: string): Promise<void>;
    clear(): Promise<void>;
}
/**
 * @hidden
 */
export declare class WebStorageProvider implements StorageProvider {
    private keyPrefix;
    constructor(keyPrefix?: string);
    get(key: string): Promise<string | null>;
    set(key: string, value: string): Promise<void>;
    remove(key: string): Promise<void>;
    clear(): Promise<void>;
    private getKey;
}
/**
 * @hidden
 */
export declare class NativeStorageProvider implements StorageProvider {
    get(key: string): Promise<string | null>;
    set(key: string, value: string): Promise<void>;
    remove(key: string): Promise<void>;
    clear(): Promise<void>;
}
