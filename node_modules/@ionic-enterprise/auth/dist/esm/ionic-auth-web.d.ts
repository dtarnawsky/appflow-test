import { IIonicAuth, AuthResult, IonicAuthOptions, IHandlers } from './interfaces';
import { IonicBaseAuth } from './base-auth';
export declare class IonicAuthWeb<IDToken extends {} = any> extends IonicBaseAuth<IDToken> implements IIonicAuth<IDToken> {
    options: IonicAuthOptions;
    protected handlers: IHandlers;
    protected logTag: string;
    constructor(options: IonicAuthOptions, handlers: IHandlers);
    protected internalGetToken(_codeName: string, _code: string, _grantType: string, _verifier: any, scope: string | undefined): Promise<AuthResult>;
    internalHandleCallback(url: string, externalCallback: boolean): Promise<AuthResult>;
    refreshSession(tokenName?: string): Promise<void>;
    private handleAuthResult;
    private postToken;
    private hiddenLoadUrl;
    protected showUrl(url: string, _options?: any, urlToCloseWindow?: string): Promise<{
        event: string;
        callback: string;
    } | void>;
}
