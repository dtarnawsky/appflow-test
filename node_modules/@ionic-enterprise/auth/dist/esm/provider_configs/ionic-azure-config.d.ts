import { IonicAuthConfig, IonicAuthOptions } from '../interfaces';
import { UrlInfo } from '../UrlInfo';
export declare class IonicAzureConfig extends IonicAuthConfig {
    options: IonicAuthOptions;
    constructor(options: IonicAuthOptions);
    getAuthorizeUrl(nonce: string, challenge: string, parameters: {
        [id: string]: string;
    }): Promise<UrlInfo>;
    getLogoutUrl(): Promise<UrlInfo>;
    getTokenUrl(): Promise<UrlInfo>;
}
