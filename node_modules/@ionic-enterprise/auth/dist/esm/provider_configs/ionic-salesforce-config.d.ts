import { IonicGeneralAuthConfig } from './ionic-general-auth-config';
import { UrlInfo } from '../UrlInfo';
export declare class IonicSalesForceAuthConfig extends IonicGeneralAuthConfig {
    generateChallengeAndVerifier(): Promise<{
        verifier: string;
        challenge: string;
    }>;
    getLogoutUrl(): Promise<UrlInfo>;
}
